# 開発セッション記録 - 2025年11月11日

## セッション概要
AIminerプロジェクトのGitHub管理開始と、脳ファイルシステムの基本実装を完了したセッション。

---

## 実施内容

### 1. GitHub環境のセットアップ

#### GitHub CLIログイン
- **課題**: 以前ログインしていたが、セッション開始時にログアウト状態だった
- **対応**: `gh auth login` で再ログイン
  - 認証コード: `5651-13A1`
  - ログインユーザー: `imuzen127`

#### GitHubリポジトリの作成
```bash
gh repo create AIminer --public --source=. \
  --description "AI Minecraft bot plugin for Paper 1.21"
```
- リポジトリURL: https://github.com/imuzen127/AIminer

#### Git初期設定
```bash
git init
git config --global user.name "imuzen127"
git config --global user.email "imuzen127@users.noreply.github.com"
```

---

### 2. リポジトリ構造の再編成

#### 要件
総合作業場として以下の構造に整理：
- `plugin/AIminer/` - プラグイン本体
- `datapack/manekinwalk_datapack/` - データパック本体
- `conversation-logs/` - 会話履歴・プロジェクトメモ

#### 実施した作業
1. `plugin/AIminer/` ディレクトリを作成
2. 既存のプラグインファイルを `plugin/AIminer/` に移動
3. データパックを `C:\Users\imuze\AppData\Roaming\.minecraft\saves\コピー用ワールド 62\datapacks\manekinwalk_datapack` から `datapack/` にコピー
4. プロジェクトメモを `conversation-logs/` にコピー
5. README.mdを作成
6. ルートの.gitignoreを更新

#### コミット
```
Reorganize repository structure: separate plugin, datapack, and conversation logs
```

---

### 3. 脳ファイルシステムの設計

#### 脳ファイルの仕様策定

**脳ファイルの4つのセクション**:
1. **rules** (約束ごと) - AIの本能部分、読み書きルール
2. **vision** (視覚情報) - チャット＋ブロック情報
3. **memory** (記憶情報) - 重要情報の記録
4. **tasks** (タスク情報) - 実行する行動

**視覚情報の要件**:
- **チャット**: サーバー内プレイヤーの発言（最新20件）
- **ブロック**: ボットの見えている範囲のブロック情報
  - プレイヤーと同じように視界に基づいて取得
  - 手前のブロックで遮られる場合は透視不可
  - レイキャスト方式で実装

**タスクの種類**:
- コマンド系: `MINE_WOOD`, `MINE_STONE`, `MOVE_TO`
- データ取得系: `GET_INVENTORY`, `GET_POSITION`, `GET_ENTITY_POSITION`
- その他: `CHAT`, `READ_MEMORY`

#### 設計ドキュメント作成
- `brain_file_spec.md` - 脳ファイルの詳細仕様
- `vision_system_design.md` - 視覚情報取得システムの設計
- `task_format.md` - タスク情報のフォーマット定義
- `brain_sample.json` - 脳ファイルのサンプル

---

### 4. プラグイン実装

#### 依存関係の追加
`build.gradle` にGsonライブラリを追加:
```gradle
implementation("com.google.code.gson:gson:2.11.0")
```

#### データモデルクラス (11個)

**Enum型**:
- `TaskType` - タスクの種類
- `TaskStatus` - タスクのステータス (PENDING, IN_PROGRESS, COMPLETED, FAILED)

**データクラス**:
- `Task` - タスク情報
- `ChatMessage` - チャットメッセージ
- `Position` - 3D座標
- `ViewDirection` - 視線方向 (yaw, pitch)
- `VisibleBlock` - 視界内のブロック
- `BlockVisionData` - ブロック視覚データ
- `VisionData` - 視覚情報統合 (チャット + ブロック)
- `Memory` - 記憶情報
- `BrainRules` - 約束ごと
- `BrainData` - 脳ファイル全体

#### コアシステム

**BrainFileManager** (`brain/BrainFileManager.java`)
- 脳ファイルの読み書き
- JSON形式でのデータ永続化
- チャットメッセージ追加 (最新20件管理)
- ブロック視覚情報更新
- メモリ情報更新
- タスク管理 (追加、ステータス更新、削除)

主要メソッド:
```java
void initializeBrainFile()
BrainData loadBrainFile()
void saveBrainFile()
void addChatMessage(String player, String message, long timestamp)
void updateBlockVision(BlockVisionData blockVisionData)
void updateMemory(String key, Object value)
void addTask(Task task)
Task getNextPendingTask()
void updateTaskStatus(int taskId, TaskStatus status)
```

**ChatListener** (`listener/ChatListener.java`)
- Paper APIの `AsyncChatEvent` を監視
- プレイヤーのチャット発言をキャプチャ
- BrainFileManagerに追加

#### メインプラグインクラスの更新

`AIminer.java` の更新内容:
- プラグイン起動時に `BrainFileManager` を初期化
- 脳ファイルを読み込み（存在しない場合は新規作成）
- `ChatListener` をイベントリスナーとして登録
- プラグイン終了時に脳ファイルを保存

```java
@Override
public void onEnable() {
    brainFileManager = new BrainFileManager(getDataFolder());
    brainFileManager.loadBrainFile();

    getServer().getPluginManager().registerEvents(
        new ChatListener(brainFileManager),
        this
    );
}

@Override
public void onDisable() {
    if (brainFileManager != null) {
        brainFileManager.saveBrainFile();
    }
}
```

---

### 5. Git管理

#### コミット内容
```
Add brain file system and chat listener implementation

- Created data model classes for brain file structure
- Implemented BrainFileManager for brain file I/O operations
- Added ChatListener to capture player chat messages
- Updated main plugin class to initialize brain system
- Added Gson dependency for JSON handling
- Created comprehensive design documentation
```

#### プッシュ
GitHubリポジトリへプッシュ完了（メモリ警告は出たが成功）

---

## 成果物

### 実装されたファイル一覧

**設計ドキュメント** (4ファイル):
- `conversation-logs/brain_file_spec.md`
- `conversation-logs/vision_system_design.md`
- `conversation-logs/task_format.md`
- `conversation-logs/brain_sample.json`

**データモデル** (11ファイル):
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/TaskType.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/TaskStatus.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/Task.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/ChatMessage.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/Position.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/ViewDirection.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/VisibleBlock.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/BlockVisionData.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/VisionData.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/Memory.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/BrainRules.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/model/BrainData.java`

**コアシステム** (2ファイル):
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/brain/BrainFileManager.java`
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/listener/ChatListener.java`

**設定ファイル**:
- `plugin/AIminer/build.gradle` (Gson依存関係追加)
- `plugin/AIminer/src/main/java/plugin/midorin/info/aIminer/AIminer.java` (更新)

---

## 現在の機能

### ✅ 動作する機能
1. プラグイン起動時に脳ファイル（`brain.json`）を生成
2. プレイヤーのチャットを自動記録（最新20件を保持）
3. プラグイン終了時に脳ファイルを自動保存
4. JSON形式での脳データの読み書き

### 🔨 未実装の機能
1. **ブロック視覚情報の取得**
   - ボット周囲のブロックスキャン
   - 視野角・遮蔽判定の実装
   - レイキャスト方式の実装

2. **AIサーバー連携**
   - 脳ファイルをAIサーバーに送信
   - AIサーバーからの応答受信
   - 脳ファイルの更新

3. **タスク実行システム**
   - タスクキューの処理
   - データパックコマンドの実行
   - タスク完了判定

4. **データパックコマンド実行**
   - `/function imuzen127x74:xoak` (木を掘る)
   - `/function imuzen127x74:xstone` (石を掘る)
   - `/function imuzen127x74:xaim` (移動)
   - `/data get entity` (データ取得)

---

## 今後の開発方針

### Phase 1: 視覚情報取得の実装
1. ボットエンティティの検出
2. 簡易版ブロックスキャン（周囲10ブロック立方体）
3. 視野角の実装
4. 遮蔽判定の実装
5. 定期更新タスクの実装

### Phase 2: タスク実行システム
1. TaskExecutorクラスの実装
2. コマンド実行機能
3. データ取得機能
4. タスク完了判定

### Phase 3: AIサーバー連携
1. AIサーバーの仕様確定
2. HTTP通信モジュール
3. 脳ファイル送信・受信
4. エラーハンドリング

### Phase 4: 統合テスト
1. ボット召喚から行動までの一連のフロー
2. 複数タスクの連続実行
3. パフォーマンス最適化

---

## 技術的な課題

### 解決済み
- ✅ Git/GitHub環境のセットアップ
- ✅ リポジトリ構造の整理
- ✅ JSON形式の脳ファイル設計
- ✅ 基本的なデータモデルの実装
- ✅ チャット監視機能

### 未解決
- ❓ AIサーバーの具体的な仕様（URL, API形式など）
- ❓ ブロックスキャンのパフォーマンス最適化
- ❓ タスク実行中の待機時間管理
- ❓ エラー発生時のリトライ戦略

---

## 参考情報

### データパック側の既存コマンド
```mcfunction
# ボット召喚
/function imuzen127x74:summanekin  # 見た目召喚
/function imuzen127x74:sumpig      # 足召喚

# ボットの動作
/function imuzen127x74:xoak {x:-13,y:-55,z:47}     # 木を掘る（常時実行）
/function imuzen127x74:xstone {x:-3,y:-60,z:46}    # 石を掘る（常時実行）
/function imuzen127x74:xaim {x:-3,y:-60,z:48}      # 移動（一度だけ）

# データ取得
/data get entity @e[tag=test1,limit=1] data        # ボットデータ
/data get entity @e[tag=test1,limit=1] data.Inventory  # インベントリ
```

### 環境情報
- **Minecraft**: Paper 1.21
- **Java**: 21
- **Gradle**: 8.x
- **IDE**: IntelliJ IDEA
- **OS**: Windows (MSYS_NT-10.0-26100)

---

## 次回セッションの予定

1. ブロックスキャン機能の実装
2. ボットエンティティの検出機能
3. 視覚情報の定期更新タスク

または

1. AIサーバーの仕様決定
2. HTTP通信モジュールの実装

---

## メモ

- Gitプッシュ時に「メモリリソース不足」のエラーが出るが、実際にはプッシュは成功している
- 脳ファイルはプラグインのdataフォルダ（`plugins/AIminer/brain.json`）に保存される
- チャット履歴は最新20件のみ保持する仕様
